# EmpaticaEmbracePlusInitialize

This is the code to initialize and collect data from Empatica Embrace Plus. You can directly run the bash script "run.sh" directly in a OS X terminal to start the program.